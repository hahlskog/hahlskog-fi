// Admin UI scaffold only. Authentication and publishing will be connected
// to a secure backend in the next deployment step.
document.getElementById('login-form')?.addEventListener('submit', (event) => event.preventDefault());
